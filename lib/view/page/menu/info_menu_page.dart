import 'package:flutter/material.dart';

class InfoMenuPage extends StatelessWidget {
  const InfoMenuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Ini Info Menu Page");
  }
}